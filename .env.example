# Google AI Studio key: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Matches docker-compose.yml — only change this if you edit the compose file's ports/credentials
DATABASE_URL=postgresql+psycopg://equi:equi@localhost:5433/equi_docintel

# Google Cloud OAuth client (Drive API) — see README "Google Drive setup" section
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
GOOGLE_OAUTH_REDIRECT_URI=http://localhost:8000/drive/oauth2callback

FRONTEND_ORIGIN=http://localhost:3000
